<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            color: #212529;
        }
        .container {
            margin-top: 30px;
            padding: 30px;
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .btn-custom {
            background-color: #000;
            border-color: #000;
            color: #ffffff;
        }
        .btn-custom:hover {
            background-color: #333;
            border-color: #333;
        }
        .btn-logout {
            background-color: #000;
            border-color: #000;
            color: #ffffff;
        }
        .btn-logout:hover {
            background-color: #333;
            border-color: #333;
        }
        .info-section h4 {
            color: #000;
        }
        .header {
            color: #000;
        }
    </style>
</head>
<body>
    <% include('../layouts/header.ejs') %>

    <div class="container">
        <h1 class="text-center header mb-4">Admin Home</h1>
        <h3 class="text-center mb-4">Welcome, <%= admin.name %></h3>

        <div class="text-center mb-4">
            <button type="button" onclick="window.location.href='/admin/dashboard'" class="btn btn-custom btn-lg">Dashboard</button>
        </div>

        <% if (typeof admin !== 'undefined') { %>
            <div class="text-center mb-4 info-section">
                <h4>Name: <%= admin.name %></h4>
                <h4>Email: <%= admin.email %></h4>
                <h4>Mobile: <%= admin.mobile %></h4>
            </div>
        <% } %>

        <div class="text-center">
            <button type="button" onclick="window.location.href='/admin/logout'" class="btn btn-logout btn-lg">Logout</button>
        </div>
    </div>

    <% include('../layouts/footer.ejs') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-9NDMJQ4p5ENPGrDZ7hsOQaw5gBwkU7UJc3SO7tC/6bCSmjfEmC9A2S4iwlQX5ny8" crossorigin="anonymous"></script>
</body>
</html>
