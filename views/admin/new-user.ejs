<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New User</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f5f6f7;
            color: #333;
        }
        .container {
            margin-top: 30px;
            padding: 20px;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
        }
        .form-control {
            border-radius: 6px;
            border: 1px solid #ced4da;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            border-radius: 6px;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }
        .alert-danger {
            color: #721c24;
            background-color: #f8d7da;
            border-color: #f5c6cb;
        }
        .form-heading {
            color: #007bff;
            font-weight: 600;
            margin-bottom: 20px;
        }
        .btn-back {
            background-color: #6c757d;
            border-color: #6c757d;
        }
        .btn-back:hover {
            background-color: #5a6268;
            border-color: #4e555b;
        }
    </style>
</head>
<body>
    <% include('../layouts/header.ejs') %>

    <div class="container">
        <h1 class="text-center form-heading">Add New User</h1>

        <form action="" method="post">
            <div class="mb-4">
                <label for="name" class="form-label">Name</label>
                <input type="text" id="name" name="name" placeholder="Enter Name" required class="form-control" />
            </div>

            <div class="mb-4">
                <label for="email" class="form-label">Email</label>
                <input type="email" id="email" name="email" placeholder="Enter Email" required class="form-control" />
            </div>

            <div class="mb-4">
                <label for="mno" class="form-label">Mobile Number</label>
                <input type="text" id="mno" name="mno" placeholder="Enter Mobile Number" pattern="\d{10}" title="Please enter exactly 10 digits." required class="form-control" />
            </div>

            <div class="mb-4">
                <label for="password" class="form-label">Password</label>
                <input type="password" id="password" name="password" placeholder="Enter Password" required class="form-control" />
            </div>

            <button type="submit" class="btn btn-primary w-100 mb-3">Register</button>

            <% if (message) { %>
                <div class="alert alert-danger text-center">
                    <%= message %>
                </div>
            <% } %>
        </form>

        <button type="button" class="btn btn-back w-100" onclick="window.location.href='/admin/dashboard'">Back</button>
    </div>

    <% include('../layouts/footer.ejs') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-9NDMJQ4p5ENPGrDZ7hsOQaw5gBwkU7UJc3SO7tC/6bCSmjfEmC9A2S4iwlQX5ny8" crossorigin="anonymous"></script>
</body>
</html>
