<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Registration Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            color: #212529;
        }
        .container {
            margin-top: 30px;
            padding: 30px;
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border: 1px solid #000;
        }
        .btn-primary-custom {
            background-color: #000;
            border-color: #000;
            color: #fff;
        }
        .btn-primary-custom:hover {
            background-color: #333;
            border-color: #333;
        }
        .btn-secondary-custom {
            background-color: #eee;
            border-color: #ccc;
            color: #000;
        }
        .btn-secondary-custom:hover {
            background-color: #ddd;
            border-color: #bbb;
        }
        .form-heading {
            color: #000;
        }
        .alert-success {
            color: #155724;
            background-color: #d4edda;
            border-color: #c3e6cb;
        }
    </style>
</head>
<body>
    <% include('../layouts/header.ejs') %>

    <div class="container">
        <h1 class="text-center form-heading mb-4">User Registration Form</h1>

        <form action="" method="post">
            <div class="mb-3">
                <input type="text" name="name" placeholder="Enter Name" required class="form-control border-dark" />
            </div>

            <div class="mb-3">
                <input type="email" name="email" placeholder="Enter Email" required class="form-control border-dark" />
            </div>

            <div class="mb-3">
                <input type="text" name="mno" placeholder="Enter Mobile Number" pattern="\d{10}" title="Please enter exactly 10 digits." required class="form-control border-dark" />
            </div>

            <div class="mb-4">
                <input type="password" name="password" placeholder="Enter Password" required class="form-control border-dark" />
            </div>

            <button type="submit" class="btn btn-primary-custom w-100 mb-3">Register</button>

            <p class="text-center mb-3">Already have an account?</p>
            <button type="button" onclick="window.location.href='/'" class="btn btn-secondary-custom w-100">Log In</button>

            <% if (message) { %>
                <div class="alert alert-success text-center mt-3">
                    <%= message %>
                </div>
            <% } %>
        </form>
    </div>

    <% include('../layouts/footer.ejs') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-9NDMJQ4p5ENPGrDZ7hsOQaw5gBwkU7UJc3SO7tC/6bCSmjfEmC9A2S4iwlQX5ny8" crossorigin="anonymous"></script>
</body>
</html>
